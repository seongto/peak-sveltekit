export interface NewRecommendationResource {
    name: String,
    description: String,
    latitude: Number,
    longitude: Number
}

export interface CompanyInfo {
    name: string;
    description: string;
};