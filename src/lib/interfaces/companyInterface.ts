export interface NewRecommendationResource {
    name: String,
    description: String,
    latitude: Number,
    longitude: Number
}

export interface NewCompany {
    name: string;
    description: string;
};